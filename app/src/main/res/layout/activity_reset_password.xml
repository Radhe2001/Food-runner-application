<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.FogotPasswordActivity"
    android:background="#000000">

    <ImageView
        android:id="@+id/imgIconResetPage"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/img"/>

    <TextView
        android:id="@+id/txtDescriptionResetPage"
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:text="@string/enter_the_new_password_below"
        android:textColor="#FFD3A3"
        android:textSize="22sp"
        android:textStyle="italic"
        android:layout_below="@id/imgIconResetPage"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"/>


    <EditText
        android:id="@+id/etPasswordResetPage"
        android:layout_width="350dp"
        android:layout_height="50dp"
        android:inputType="textPassword"
        android:maxLength="10"
        android:maxLines="1"
        android:layout_marginTop="30dp"
        android:layout_below="@id/txtDescriptionResetPage"
        android:layout_centerHorizontal="true"
        android:hint="@string/password"
        android:textColor="#000000"
        android:background="#ffffff"
        android:textSize="20sp"
        android:textStyle="italic"/>

    <EditText
        android:id="@+id/etConfirmPasswordReset"
        android:layout_width="350dp"
        android:layout_height="50dp"
        android:inputType="textPassword"
        android:layout_marginTop="15dp"
        android:layout_below="@id/etPasswordResetPage"
        android:layout_centerHorizontal="true"
        android:hint="@string/confirm_password"
        android:textColor="#000000"
        android:background="#ffffff"
        android:textSize="20sp"
        android:textStyle="italic"/>

    <Button
        android:id="@+id/btnNextReset"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:text="@string/next"
        android:textSize="24sp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="40dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:textStyle="italic"
        android:background="#FC2947"/>


</RelativeLayout>